services:
  bekendchi-app:
    build: .
    volumes:
      - .:/app
      - bekendchi-static:/app/staticfiles  # Statik fayllar uchun
      - bekendchi-media:/app/media  # Media fayllar uchun
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - bekendchi-network

  bekendchi-nginx:
    image: nginx:latest
    ports:
      - "7000:7000"  # HTTP uchun 7000-port
      - "8443:8443"  # HTTPS uchun 8443-port
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - bekendchi-static:/app/staticfiles  # Statik fayllar
      - bekendchi-media:/app/media  # Media fayllar
      - /etc/letsencrypt:/etc/letsencrypt:ro  # SSL sertifikat fayllari
    depends_on:
      - bekendchi-app
    networks:
      - bekendchi-network

networks:
  bekendchi-network:
    driver: bridge

volumes:
  bekendchi-static:
  bekendchi-media: